#include <stdio.h>

float want_water(float synolo)
{	
	

	float nsynolo;
	float nero;
	nero = 0.5 ;
	nsynolo = synolo + nero;
	printf("Το νέο σύνολο είναι %.2f €",nsynolo);
	
	
return(nsynolo);
}


float rest(float synolo,float money)
{
	if(money < nsynolo){
		printf("Τα χρήματα δεν φτάνουν.\nΠαρακαλώ εισάγετε χρήματα.\n");
		
	}

	float resta,nsynolo;
	float nero;
	nero = 0.5 ;
	nsynolo = synolo + nero;
	resta = money - nsynolo;
	printf("Τα ρέστα σας είναι %.2f €",resta);
	return(resta);
}
